import {ReportText} from './Text.styled'

function TextReportButton() {
  const text = 'Перейти к отчётам'
    return (
        <ReportText> 
          {text}
        </ReportText>
    );
  }
  export {TextReportButton};